<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ต้องมี INTERNET เพื่อโหลดเว็บ + เรียก Claude API + WalletConnect relay -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- กล้อง/ไมค์ สำหรับผู้ช่วย AI (เสียง+วิสัยทัศน์) -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="PuppyPocket"
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        android:usesCleartextTraffic="false"
        tools:targetApi="31">

        <activity
            android:name="com.puppypocket.wallet.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|keyboardHidden">

            <!-- ไอคอนแอปปกติ -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep link scheme: puppypocket:// — ให้กระเป๋าเงิน (MetaMask/Trust/Bitget ฯลฯ)
                 ส่งกลับมาที่แอปนี้โดยตรงหลังผู้ใช้กดอนุมัติเชื่อมต่อ ตรงกับ metadata.redirect.native ในเว็บ -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="puppypocket" />
            </intent-filter>

            
        </activity>

    </application>
</manifest>
