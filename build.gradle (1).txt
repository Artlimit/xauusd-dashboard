plugins {
    id 'com.android.application'
    id 'org.jetbrains.kotlin.android'
}

android {
    namespace 'com.puppypocket.wallet'
    compileSdk 34

    defaultConfig {
        applicationId "com.puppypocket.wallet"
        minSdk 24            // Android 7.0+ (ครอบคลุมมือถือส่วนใหญ่ที่ยังใช้งานอยู่)
        targetSdk 34
        versionCode 1
        versionName "1.0.0"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
    kotlinOptions {
        jvmTarget = '17'
    }
}

dependencies {
    implementation 'androidx.core:core-ktx:1.13.1'
    implementation 'androidx.activity:activity-ktx:1.9.1'
    // ไม่ต้องมี dependency เพิ่มสำหรับ WebView เอง เพราะเป็นส่วนหนึ่งของ Android framework อยู่แล้ว
}
